name: google_ml_kit_example
description: Demonstrates how to use the google_ml_kit plugin.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_pdfview: ^1.3.3
  image_picker: ^1.1.2
  camera: ^0.10.6
  path: ^1.9.0
  path_provider: ^2.1.4
  google_ml_kit: ^0.19.0
  google_mlkit_barcode_scanning: ^0.13.0
  google_mlkit_digital_ink_recognition: ^0.13.0
  google_mlkit_document_scanner: ^0.3.0
  google_mlkit_face_detection: ^0.12.0
  google_mlkit_subject_segmentation: ^0.0.1

  # Replace local paths with pub.dev versions
  # google_ml_kit: ^0.7.0  # Adjust the version as per the latest available.
  # google_mlkit_barcode_scanning: ^0.3.0
  # google_mlkit_digital_ink_recognition: ^0.3.0
  # google_mlkit_document_scanner: ^0.3.0
  # google_mlkit_face_detection: ^0.3.0
  # google_mlkit_face_mesh_detection: ^0.3.0
  # google_mlkit_image_labeling: ^0.3.0
  # google_mlkit_object_detection: ^0.3.0
  # google_mlkit_pose_detection: ^0.3.0
  # google_mlkit_selfie_segmentation: ^0.3.0
  # google_mlkit_subject_segmentation: ^0.3.0
  # google_mlkit_text_recognition: ^0.3.0
  # google_mlkit_entity_extraction: ^0.3.0
  # google_mlkit_language_id: ^0.3.0
  # google_mlkit_translation: ^0.3.0
  # google_mlkit_smart_reply: ^0.3.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/ml/
    - assets/images/
